{
  "name": "codex-claude-usage-plugin",
  "version": "1.0.0",
  "description": "Windows floating widget showing Claude and Codex subscription usage",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "electron-builder"
  },
  "devDependencies": {
    "canvas": "^3.2.2",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
